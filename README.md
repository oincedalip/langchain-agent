# langchain-agent
This is an agent built by langchain and has websearch capabilities

Install the dependencies with the following commands:

```
uv python install 3.12.7
uv venv --python 3.12.7
.venv\Scrpits\activate
uv sync
```
